{
  "name": "whitehouse-expense-management",
  "version": "2.0.0",
  "private": true,
  "description": "Whitehouse - React + TypeScript + Django REST Framework + PostgreSQL (Amazon RDS) + AWS",
  "scripts": {
    "dev:client": "npm --prefix client run dev",
    "build:client": "npm --prefix client run build",
    "postinstall": "npm --prefix client install",
    "test:server": "python server/manage.py test",
    "seed:server": "python server/manage.py seed_db"
  }
}
